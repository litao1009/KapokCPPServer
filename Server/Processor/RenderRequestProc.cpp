#include "RenderRequestProc.h"

IMPLEMNET_REFLECTION_WITH_KEY(RenderRequestProc, RenderRequest)

void RenderRequestProc::Process(const beast::websocket::opcode& op, WSSessionSPtr& session, const Ptree& content)
{
	
}
